server:
  port: ${PORT:8080}

spring:
  application:
    name: @project.artifactId@
    version: @project.version@
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:provisioner}
    password: ${RABBITMQ_PASSWORD:provisioner}
    virtual-host: ${RABBITMQ_VIRTUAL_HOST:/}
  docker:
    compose:
      enabled: false
app:
  security:
    api-key-enabled: ${SECURITY_API_KEY_ENABLED:false}
    api-key: ${SECURITY_API_KEY:}
    api-key-header: ${SECURITY_API_KEY_HEADER:X-API-Key}
    public-paths:
      - /actuator/health
  proxy:
    # External AMQP URL that proxies use to connect (may differ from internal spring.rabbitmq.host).
    amqp-url: ${PROXY_AMQP_URL:amqp://localhost:5672}
    username: ${PROXY_RABBITMQ_USERNAME:${spring.rabbitmq.username}}
    password: ${PROXY_RABBITMQ_PASSWORD:${spring.rabbitmq.password}}
    exchange: ${PROXY_EXCHANGE:ilm-proxy}
    response-queue: ${PROXY_RESPONSE_QUEUE:core}
    request-routing-key-prefix: coremessage.
    response-routing-key-prefix: proxymessage.
    helm-install-template: classpath:templates/helm.install.template
    token-config:
      # Must be at least 32 characters.
      signing-key: ${TOKEN_SIGNING_KEY:}
      subject: proxy-config
      version: 1
      config-template: classpath:templates/rabbitmq.proxy_config.template

logging:
  pattern:
    correlation: "%X"
  level:
    com.otilm.provisioning.rabbitmq: INFO
    org.springframework.amqp: INFO
